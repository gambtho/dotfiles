---
description: Audit, re-engineer, or bootstrap a project with AI-first design principles
---

Load the ai-firstify skill and use it to analyze this project.

If $ARGUMENTS contains "audit", "review", "analyze", or "assess", run in Audit mode (read-only analysis).
If $ARGUMENTS contains "fix", "improve", "transform", or "re-engineer", run in Re-Engineer mode (active changes).
If $ARGUMENTS contains "bootstrap", "new", "start", or "setup", run in Bootstrap mode (new project).
If no arguments are provided, ask the user which mode they want.

Follow the full procedure described in the ai-firstify skill, including loading reference files on demand and running the validation script on any generated report.
