---
description: Review open PRs with no human comments — now a skill, auto-invoked when relevant
---

This command has been converted to a skill and is now auto-invoked when relevant.
You can also ask directly: "review open PRs" or "check unreviewed PRs".

If you're seeing this, invoke the `review-prs` skill instead.
