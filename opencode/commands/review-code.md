---
description: Review code changes since a commit — now a skill, auto-invoked when relevant
---

This command has been converted to a skill and is now auto-invoked when relevant.
You can also ask directly: "review my code since main" or "check my changes".

If you're seeing this, invoke the `review-code` skill instead.
