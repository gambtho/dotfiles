---
description: Analyze a PR's open comments and failing CI — now a skill, auto-invoked when relevant
---

This command has been converted to a skill and is now auto-invoked when relevant.
You can also ask directly: "fix PR #123" or "address the PR comments on #123".

If you're seeing this, invoke the `fix-pr` skill instead.
