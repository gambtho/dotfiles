# Personal overlay placeholder

This file exists so `claude-link-project --local-md` has something to point at.
The project's actual shared rules live in `AGENTS.md` (tracked) at the repo
root. Personal extensions live in `AGENTS.md` next to this file (loaded into
the project via the `AGENTS.local.md` import shim).
