.DS_Store
*~
*.swp

# Personal Claude Code config that lives alongside project repos via symlink
# (see ~/.dotfiles/projects/). Keep it out of public/open-source commits.
.claude/
CLAUDE.md
CLAUDE.local.md
AGENTS.local.md

# Personal docker-compose overrides (written by my:project-claude-setup skill).
# Compose auto-merges docker-compose.override.yml; treat it as host-specific.
docker-compose.override.yml
docker-compose.override.yaml

# Other AI assistant scratch / config
.aider*
.cursor/
# Superpowers subagent-driven-development scratch: per-plan ledgers, briefs and
# review packages. Regenerable, and never part of a PR diff.
.superpowers/

# Throwaway Playwright config used to pin a worktree's e2e run to a private
# port with reuseExistingServer:false. Must never reach a commit.
playwright.local.config.ts

**/.claude/settings.local.json
